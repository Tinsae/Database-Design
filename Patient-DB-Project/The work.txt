-- -- DROP TABLE PATIENT_ID;
-- CREATE TABLE PATIENT_ID (
-- 	PATIENT_ID_CODE INT (6) NOT NULL AUTO_INCREMENT PRIMARY KEY,
-- 	PATIENT_ID_DESC VARCHAR (20)
-- );
-- DROP TABLE PATIENT;
-- -- CREATE TABLE PATIENT (
-- -- 	PATIENT_ID INT AUTO_INCREMENT NOT NULL PRIMARY KEY,
-- -- 	PATIENT_ID_DESC VARCHAR (20),
-- -- 	PATIENT_FIRST VARCHAR (40) NULL DEFAULT NULL,
-- -- 	PATIENT_LAST VARCHAR (40) NULL DEFAULT NULL,
-- -- 	PATIENT_AGE INT (20) NOT NULL,
-- -- 	PATIENT_ID_CODE INT (6) NOT NULL
-- );
-- 
DROP TABLE
IF EXISTS BABY;

CREATE TABLE BABY (
	PATIENT_ID INT NOT NULL PRIMARY KEY,
	BABY_WEIGHT FLOAT (2, 2),
	CONSTRAINT FK_pateint_baby FOREIGN KEY (PATIENT_ID) REFERENCES PATIENT (PATIENT_ID)
);

DROP TABLE
IF EXISTS ADOLESCENT;

CREATE TABLE ADOLESCENT (
	PATIENT_ID INT NOT NULL PRIMARY KEY,
	ADOLESCENT_GRADE VARCHAR (50),
	CONSTRAINT FK_pateint_adolescent FOREIGN KEY (PATIENT_ID) REFERENCES PATIENT (PATIENT_ID)
);

DROP TABLE
IF EXISTS ELDERLY;

CREATE TABLE ELDERLY (
	PATIENT_ID INT,
	ELDERLY_DISA VARCHAR (50),
	PRIMARY KEY(PATIENT_ID),
	CONSTRAINT FK_pateint_elderly FOREIGN KEY (PATIENT_ID) REFERENCES PATIENT (PATIENT_ID)
);


DROP TABLE
IF EXISTS ELDERLY_TAKER;

DROP TABLE
IF EXISTS CARETAKER;



CREATE TABLE CARETAKER (
	CARETAKER_ID INT PRIMARY KEY,
	CARETAKER_FIRST VARCHAR (50),
	CARETAKER_LAST VARCHAR (50)
);



CREATE TABLE ELDERLY_TAKER (
	PATIENT_ID INT,
	CARETAKER_ID INT NOT NULL,
	PRIMARY KEY(PATIENT_ID, CARETAKER_ID),
	CONSTRAINT FK_caretaker_elderlytaker FOREIGN KEY (CARETAKER_ID) REFERENCES CARETAKER (CARETAKER_ID),
	CONSTRAINT FK_elderly_elderlytaker FOREIGN KEY (PATIENT_ID) REFERENCES ELDERLY (PATIENT_ID)
);

